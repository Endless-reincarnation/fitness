# 进度记录

## 当前状态

- 小程序已接入真实 AppID 并成功初始化云开发环境。
- 云环境 ID：`dev-d1getmtzq8dd4414c`。
- **云开发数据联调已全线通畅**：云端已成功创建 14 个数据库集合，并按规划完成了动作、肌群、计划模板的种子数据导入。云数据读写权限配置就绪，且已通过「云数据诊断」大屏完整验证（均显示正常）。
- **训练流程与数据打卡看板重大升级**：
  - 数据页打卡日历实现周/月视图折叠切换（默认以 7 天周视图形式高效利用垂直空间，点击底栏可平滑展开为完整月历，周期切换导航已自适应按周或按月滑动）。
  - 页面布局完成“上训练、下体重”层级重组，优先呈现最核心的打卡和训练报告。
  - 完成了上次训练数据（重量、次数）首组带入、下放整合动作指南、休息时间快捷微调、屏幕常亮与多重触感震动反馈。
- **动作库与计划模板重大扩充**：
  - 官方动作种子库已从 15 个大幅扩充至 72 个最常用的黄金训练动作，涵盖胸、背、肩、腿、手臂和核心六大区域，并补齐了配套的详细中文说明、常见错误及器械分类。
  - 新增编译了 3 套官方经典计划模板（PPL推拉腿、全身循环、上下肢分化），使用统一的数据流脚本构建，自动生成了 13 个训练日与 55 个动作训练处方的种子文件。
- **页面色彩视觉解耦与主题变量重构**：
  - 将小程序内所有页面原本硬编码的黄色调透明值（`rgba(255, 210, 63, ...)`）和背景微光全数提取为全局 CSS 主题变量。
  - 彻底打磨了“黑绿科技”风和“黑黄力量”风切换时的颜色无缝契合度，消除了一切色彩混染杂色，极大提升了 UI 交互的质感。
- **管理后台（Admin）表单动态化配置**：
  - 成功完成了官方计划编排表单中适合人群（`target_users`）、执行说明（`notes`）和膳食营养建议（`nutrition`）的动态增删行与提交存储改造，彻底打通了后台的数据管理闭环。
- **AI 自定义计划 MVP 已打通**：
  - 新增 AI 计划表单页，支持目标、水平、器械、限制偏好和选填个人信息（性别、年龄、身高、体重）。
  - AI 生成改为小程序端 `wx.cloud.extend.AI` 直连，优先使用流式输出弹层展示生成过程，失败时回落本地草稿并提示原因。
  - AI 草稿可进入自定义计划编辑页，保存后保留计划说明、生成思路和注意事项，并在计划详情页展示。
  - 计划库完成“创建入口 / 正在使用 / 我的计划与官方模板页签”重组，删除当前启用计划会同步清理启用状态和训练草稿。

## 最近提交

- `3ea2b12` feat: 提取全局CSS主题色渐变与透明度变量，彻底解决黑绿皮肤黄色色彩混染问题
- `e9ca02e` feat: 动作种子库大幅扩充至72个黄金动作，添加全新3套分化模板计划并更新种子编译
- `bf23cf4` feat: 网页管理端官方计划编辑表单动态化改造，支持人群/营养建议动态增删行及保存
- `01c69e5` feat: 实现打卡日历折叠切换与页面布局优化，包含上次重量带入与反馈功能
- `17aabfe` style: 优化首页‘开始训练’按钮布局为底部固定悬浮，改善长动作列表下的交互体验
- `8b4836b` feat: 实现小程序端微信登录、头像昵称获取与云端个人资料同步
- `c843697` feat: 初始化网页后台管理端，修复云数据库种子格式与小程序上传超限

## 当前待办

- 回归验证 AI 生成真实模型在不同基础库和真机环境下的表现，必要时继续优化流式展示和 JSON 容错。
- 自定义动作第一版先存本地 `customExercises`，后续接云开发时迁移到用户私有动作集合。
- 动作分类第一版先走前端 `exerciseCategory` 工具，支持身体部位、器械分类和首字母搜索索引，后续再视规模决定是否云端维护分类。
- 最终的小程序线上提审与发布前打包校验。

## 注意事项

- 数据权限规范：官方字典集合为“所有用户可读，仅创建者可读写”；用户私有历史记录为“仅创建者可读写”以保持数据隔离。
- 自定义计划动作必须引用动作 ID；系统动作引用标准动作库，自定义动作引用用户自定义动作库，防止自由文本引起动作详情缺失。

## 未来规划 / 技术储备 (AI Mode)

- **微信小程序原生 AI 模式（AI Mode / MCP 协议）接入**：
  - **背景**：微信团队推出的智能化开发框架，支持通过自然语言在微信全局 AI 聊天界面直接触发小程序的原子接口（如“帮我开始今天的训练”），并由小程序的卡片组件渲染交互结果。
  - **行动指南**：由于目前该功能处于官方内测阶段，暂不支持线上提审。我们先将其登记为中远期规划。待微信官方开放提审发布后，可在 `feature/ai-mode` 分支中建立 Skill 分包，封装打卡与数据查询技能，抢占官方流量红利。
