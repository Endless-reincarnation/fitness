# 进度记录

## 当前状态

- 小程序已接入真实 AppID 和云开发环境。
- 云环境 ID：`dev-d1getmtzq8dd4414c`。
- 已支持云端读取计划库和动作库，本地 mock 作为兜底。
- 已支持训练记录、训练组和体重记录写入云端。
- 已增加云端写入失败重试队列，网络或权限异常时先保留本地记录。
- 已增加“我的 -> 云数据诊断”页面，用于检查集合数量和待同步记录。
- 已修复自定义计划页面加载错误。

## 最近提交

- `d442cbf` 修复自定义计划页面加载错误
- `bc5d400` 增加云数据诊断页面
- `9314e74` 补充云数据库集合配置说明
- `8d7eec9` 增加云端记录同步重试
- `48f7c0d` 支持云端保存训练和体重记录

## 当前待办

- 在云开发数据库中创建全部集合。
- 按 `seed/cloud-import/README.md` 顺序导入种子数据。
- 在“云数据诊断”页面确认集合数量：
  - `muscles` 应为 15
  - `exercises` 应为 15
  - `plan_templates` 应为 1
  - `plan_days` 应为 3
  - `plan_day_exercises` 应为 15
- 验证完成训练后 `workout_sessions` 和 `workout_sets` 是否新增记录。
- 验证记录体重后 `body_weights` 是否新增或覆盖当天记录。

## 注意事项

- 如果控制台报 `database collection not exists`，说明对应集合还没创建。
- 当前已知缺失集合报错：`plan_day_exercises`。
- 集合名必须和文档完全一致，不能用中文名或驼峰名。
- 云集合未建好前，小程序会自动回落本地数据，所以页面能打开不代表云端数据已生效。
