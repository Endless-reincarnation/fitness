# 进度记录

## 当前状态

- 小程序已接入真实 AppID 并成功初始化云开发环境。
- 云环境 ID：`dev-d1getmtzq8dd4414c`。
- **云开发数据联调已全线通畅**：云端已成功创建 14 个数据库集合，并按规划完成了动作、肌群、计划模板的种子数据导入。云数据读写权限配置就绪，且已通过「云数据诊断」大屏完整验证（均显示正常）。
- **训练流程与数据打卡看板重大升级**：
  - 数据页打卡日历实现周/月视图折叠切换（默认以 7 天周视图形式高效利用垂直空间，点击底栏可平滑展开为完整月历，周期切换导航已自适应按周或按月滑动）。
  - 页面布局完成“上训练、下体重”层级重组，优先呈现最核心的打卡和训练报告。
  - 完成了上次训练数据（重量、次数）首组带入、下放整合动作指南、休息时间快捷微调、屏幕常亮与多重触感震动反馈。

## 最近提交

- `01c69e5` feat: 实现打卡日历折叠切换与页面布局优化，包含上次重量带入与反馈功能
- `17aabfe` style: 优化首页‘开始训练’按钮布局为底部固定悬浮，改善长动作列表下的交互体验
- `8b4836b` feat: 实现小程序端微信登录、头像昵称获取与云端个人资料同步
- `c843697` feat: 初始化网页后台管理端，修复云数据库种子格式与小程序上传超限

## 当前待办

- 开发和完善「自定义计划」功能（提供多训练日新增/编辑、标准动作引用、组数/次数/休息时间编排以及启用执行的本地/云端闭环）。
- 自定义动作第一版先存本地 `customExercises`，后续接云开发时迁移到用户私有动作集合。
- 在 `seed/` 中设计并扩充更多的官方经典计划模板（推拉腿、全身性、上下肢分化等），重新编译并导入云数据库。
- 最终的小程序线上提审与发布前打包校验。

## 注意事项

- 数据权限规范：官方字典集合为“所有用户可读，仅创建者可读写”；用户私有历史记录为“仅创建者可读写”以保持数据隔离。
- 自定义计划动作必须引用动作 ID；系统动作引用标准动作库，自定义动作引用用户自定义动作库，防止自由文本引起动作详情缺失。
